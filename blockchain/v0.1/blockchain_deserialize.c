#include "blockchain.h"

/**
* blockchain_deserialize - entry to function
* Desc: blockchain_deserialize function that deserializes
* a Blockchain from a file
* @path: contains the path to a file to load the Blockchain from
* Return: deserialized Blockchain upon success, or NULL upon failure
*/
blockchain_t *blockchain_deserialize(char const *path)
{
	(void) path;
	return (NULL);
}
